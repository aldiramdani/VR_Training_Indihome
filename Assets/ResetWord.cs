using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResetWord : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        sceneManager sm = new sceneManager();
    }
    
}
